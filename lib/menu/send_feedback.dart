import 'package:flutter/material.dart';

class SendFeddback extends StatelessWidget {
  const SendFeddback({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Halaman Send Feddback'),
      ),
    );
  }
}
